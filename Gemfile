# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https:github.com/#{repo_name}}" }

# Specify your gem's dependencies in cpf_simplifier.gemspec
gemspec

gem 'rake', '~> 13.0'

gem 'rspec', '~> 3.0'
